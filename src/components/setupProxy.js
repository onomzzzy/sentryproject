/*const proxy = require("http-proxy-middleware");

module.exports = function (app) {
  app.use(
    proxy("/store/users", {
      target: "https://onomecode-security.herokuapp.com",
      changeOrigin: true,
    })
  );
};*/
